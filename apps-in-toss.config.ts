import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "byebye",
  brand: {
    primaryColor: "#3182F6",
  },
  webBundleDir: "dist",
  permissions: [],
});
